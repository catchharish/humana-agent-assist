# DECISIONS_LOG

- **Simulated REST override (Harish, 17 Sep 2026):** Per-system `/api/simulated/{benefits|eligibility|claims|pharmacy|provider|scripting}` is the only retrieval boundary; app code must not import fixtures. This overrides the contract header “source labels do not require six integrations.” Do not undo without asking. Enrollment POST 403 without human actor. Delay/404/overlay for replays.
- **Models (catalog re-check 17 Sep 2026, developers.openai.com/api/docs/models):** Fast `gpt-5.6-luna` (`reasoning.effort: none`, 1s trigger/interpretation); mid `gpt-5.6-terra` (`reasoning.effort: none` unless C05 fails); embeddings `text-embedding-3-small`. Not gpt-4o / gpt-4o-mini / gpt-5.4-mini.
- **Coverage/telephony placement:** Coverage-review sits **beside** the six (`sourceSystem: "coverage-review"`), same as telephony. Not a claims resource.
- **Router composition (Harish, 17 Sep 2026):** Multi-source questions compose routes. Historical $8/$27: structured lookup for purchases + dated classifications; policy passage from SEARCH; mock returns raw candidates including `DEMO-POLICY-OTHER-v1`; copilot filter rejects wrong-plan. Main call must show route 4 + rejection in diagnostics.
- **Quote validity:** Field is `validityStatus` (`valid` in the main run). Do not invent `validUntil`.
- **C06 / SUBMISSION.md:** Manual UI checklist, no Playwright. `SUBMISSION.md` written 18 Sep 2026 after M3.
- **Keep luna (Harish, 17 Sep 2026):** Do not switch models or relax the 1s target. Two-stage triggers: stage 1 registry-pattern code (never historical-charge; never retract a nudge); stage 2 luna on ambiguous partials. Warm-up luna at call connect. Embed documents once at startup; query-only per SEARCH.
- **Luna warm keep-alive remeasure (17 Sep 2026):** Keep-alive `/v1/responses`, `reasoning.effort: none`. Warm-up 1563 ms discarded. 10 sequential ms: 1330, 893, 1132, 757, 906, 1431, 838, 809, 1401, 843. **Median 899 ms. Max 1431 ms.** 4/10 missed 1s; record honestly.
- **M1 shipped (17 Sep 2026):** Timed stream + five regions + quiet greeting + auth gate. Disclosures via GET scripting REST. Luna warmup at connect (1232 ms this run). Pause 44486 ms excluded from machine time. Stopped before remaining REST/router/M2.
- **Warmup async (17 Sep 2026):** Luna warm-up is fire-and-forget at connect; session start does not await it.
- **Timing measure:** event-to-client-paint (`paintMs`). `machineMs` 0 is **unmeasured**, not a result. Run `3125270d-…` kept and flagged in `runs/3125270d-….flag.json`.
- **Enrollment token (not actorType):** Confirm mints a one-time server token bound to read-back scope. Pharmacy POST 403 without unused matching token. AI tools cannot mint or read tokens.
- **M2a historical delay (17 Sep 2026):** T01 injects SEARCH delay (`X-Demo-Delay-Ms`) so the $8/$27 answer lands after the refill-ready focus switch; marked injected. T06A uses overlay `T06A` (omit `DEMO-NET0818`); no injected delay.
- **Auth vs Resume:** DEMO-AUTH001 is stream system event `e-auth`. Resume only ends pause.
- **UNCERTAIN:** transcript uses a dashed `UNCERTAIN ?` text marker, not color alone. Stream includes `e-noise-uncertain`.
- **M3 replays (18 Sep 2026):** T04B in-flight quote uses overlay delay on Lakeview pharmacy-filtered GET; correction bumps `quoteGeneration` and marks the late Lakeview metformin row `invalidated` without renaming it Oak Street. T08B edit invalidates tokens; member withdraw blocks mint/submit. T03B `optionalWorkSuppressed`. COMPLETED_SERVICING does not require a transfer connection. Driver auto-resumes pauses and fires human routes from session evidence/pause labels; it never injects AI classifications.
- **Tier 2 integrity + latency (18 Sep 2026, Harish):** Registry repetition is write-once after exact DEMO-PRICING-v1 delivery; later triggers log `suppressed` and cannot change state/note (prior readback re-fire = §21.5, now fixed). Comparison copy is built from quote records. Model prompts are rules + evidence JSON. Historical promote uses stored `queryText`/`sourceUtteranceId`. No router default query. Few-shots do not reuse test-stream sentences. Overlay delays live in mock handlers / start `injectedDelayMs`; T06A offer skip is the driver. Compliance paints as soon as stage 1 / wording verify; ingest does not wait on luna for that line. DEMO-AUTH001 VALID prefetches refill/claims/network/FAST90. Structured + FAST90 answers are code. Member (and non-trigger advocate) interpret still awaited for servicing. Live UI is SSE, not 400 ms poll. Historical reported with and without the 2800 ms SEARCH delay. Targets unchanged; remaining paint misses logged as misses.
- **Freeze pass (18 Sep 2026):** One luna JSON per utterance; ingest never waits on it. FAST90/quotes from code when the utterance identifies the need. Copy templates take session parameters. Miss→exact uses stream offsetMs (4800). Wrap stable at closing; outcome at connection. Failed T02A kept `2c873d17-…`. Code frozen except rehearsal defects.
- **Governed utterance rules + readback (18 Sep 2026):** FAST90/compare-yes 14–26 ms is a conservative regex on the transcript line (clock = ingest `clientT` / driver `Date.now()` before POST, not luna). Patterns live in `fixtures/utterance_rules.json` via simulated scripting REST. Hedges/partial/compound yes wait or clarify; luna may accept only `wait`, never override `hedge`. Quote amounts render only on `absolute_yes`. Enrollment readback uses DEMO-SERVICE-v1 `displayName`; doubled-word test covers templated member-facing copy.
- **Generalized classes, not script lines (18 Sep 2026):** 90-day need = duration class (90-day / ninety-day / three-month) AND question/how cue, minus estimate/ran-out/already-on. Consent = FINAL + scoped ask pending + no hedge token + governed affirmative set. Partials never change display; luna starts but does not apply. Luna output is short enum JSON (`max_output_tokens` 128). Warm 10-call median **1511 ms** (max 2134). Scripted e-90day/e-yes-compare stay **code_rule** (14–26 ms T01). Paraphrases that miss the class go **luna** (1058 / 1685 ms). Historical +2800 still MISS.

