# DECISIONS_LOG

- **109-item eval runner (20 Sep 2026):** Standalone `scripts/eval_runner.ts` + `tests/eval_set_v1.json`. Drives live session start/ingest/state only; scores regex + Sol judge; writes `runs/eval_v1_*/`. No application code change.

- **Strip / Answer copy tweaks (20 Sep 2026):** Display only. Call Reason (no “in his words”); no Presenter only / Main call on the demo bar; no duplicate title above Now; Caller + red/green identity dot; Answer pill light green; “From user records”.

- **Unique Up-next tip keys (20 Sep 2026):** Waiting tips keyed by index+kind; `seatRecommendation` keeps one pending card per action kind so Money-saving tip cannot stack three identical rows.

- **Answer Sources used (20 Sep 2026):** `resumeParkedNeed` now restores `usedSources` onto Now (hist often parks under T01 interrupt, then returns with body only). `loadFast90` files Plan rules / lineage as `usedSources` so the 90-day card cites its document. UI also falls back to the focused need’s stored sources.

- **Clarify holds Now over tips (20 Sep 2026):** Active consent clarification owns Now. Pending NBA / objection stays in Up next until the clarify is cleared (or the advocate pins the tip). `showClarifyOnce` clears `nowCardNeedKind` so a hedge is not treated as “caller moved on” from a prior say. UI: after identity is VALID, paraphrased greeting no longer forces the legal card (same gate as `greetingWordingOnNow`), so clarify can paint. Live Main call step-mode at hedge pause (`clarify-hold2`): Now = Clarify comparison interest (Answer); Up next = Objection rebuttal tip. Rule is session-agnostic. Does not change legal / enrollment / transfer priority.

- **Clarify card hygiene (20 Sep 2026):** Clarify paints with empty sources/statements; the Answer card only cites when not on clarify (no call-wide `retrievedSources` inheritance). After identity, paraphrased greeting closes to `late_finding` on auth, and the Legal Statements rail maps post-identity due/paraphrased greeting to “said late” instead of “read now”. Live check (`polish-src1` hedge): no Sources used / From-line on clarify; rail shows Greeting — said late.

- **Legal paraphrase word marks (20 Sep 2026):** `wordDiff` is sequence-aligned (LCS), not bag-of-words presence. The legal card marks missing words in the required sentence and extra words in “What you said instead”; “What was left out” is consecutive missing phrases. Pricing paraphrase cue after a deadline nudge only accepts short recovery attempts (`prices?|change`, no dollar amount) so the estimate line that caused the miss is not labeled as the paraphrase. C01 pricing tests seed `identityStatus = VALID` (same gate as live).

- **T01 transcript race owns Now timing (20 Sep 2026):** Fixture gaps matter. Refill answers and dwells on Now; metformin starts looking up in the background; interrupt must land while that lookup is still in flight so refill stays primary and hist parks as Answer ready (T01 ready-gate: Now must not show $8/$27). `$8/$27` paints on “So, the metformin?”. Ingest no longer awaits Terra for new member questions (void the loop; streamHold paces). streamHold holds only for a lookup that owns Now — not for a background hist while refill is still on the card. Late answers still file and close their lookup clock without stealing Now. Resume of a parked say stamps a fresh answeredAt so dwell applies. Supersedes same-day “await Luna+Terra for member finals” / “hold every open lookup” under Hold + stream dwell.

- **Terra owns the answer card (20 Sep 2026):** Luna flags only label need kind. They do not decide whether an answer card or need item appears. Every member final runs Terra; `need:answer` paints and files, `need:nothing` creates neither and leaves Now as it was. Removed the occupy-Now regex and the Luna `lineNeedsAnswer` paint gate. `beginAnswerLoop` parks a prior answer onto its need but does not clear Now or put Searching on it before Terra paints. Supersedes the same-day “Paint when Luna misses” / Luna-occupy note under ready recs.

- **Hold + stream dwell for labeled asks (20 Sep 2026):** When Luna labels a need, start lookup progress and inject the advocate hold as soon as the answer loop begins. Playback holds while a Now-owning lookup is open and ~3.5s after a real say. Do not call `resumeParkedNeed` for a need that was just created on this same line with no answer — that path returned without running Terra and skipped first-refill answers. *(Ingest-await and hold-every-open-lookup superseded by T01 transcript race note above.)*

- **Now card tracks the live transcript line (20 Sep 2026):** The suggestion rail must not keep a tip on Now while the latest member question is being looked up or answered. Looking-up / answer-for-this-line wins over `recTurn`. “On screen” in Questions matches the card actually shown. Soft parked-need matches do not keep an old answer up through a non-question (objection), so tips can still take Now then.

- **Now title matches the card (20 Sep 2026):** The greeting now-card is not an answer (`origin === "answer"` only). Do not invent a lookup from `rightNowLine` (“Call connected — greet the caller”). Greeting stays on Now as legal until a real member question or lookup replaces it. The banner is the card headline. Suggestion cards show `body`. Start call no longer waits on a second disclosures GET.

- **Ready recs, nothing-needed, Harry comparison offer (20 Sep 2026):** A pending recommendation is no longer dropped (`queued` instead of `already_pending`) and takes Now after the current answer has been readable for ~3.5s, the advocate thumbs that answer, or the caller moves on. **(20 Sep later)** Dwell promotion no longer stays blocked by `answerMatchesLastLine` (that left Money-saving tip stuck in Up next for the whole hist window). Clicking a waiting tip in Up next promotes that kind onto the seat before pinning Now. Priority unchanged (legal / enrollment / transfer / clarify still win). Up next shows a count and each waiting kind. The 8s cap still times draft-to-ready, not screen wait. Non-questions: Terra `need:nothing` (not Luna) decides no card — see Terra owns the answer card. Playbooks only: if the member paid more at standard retail, is not on mail, and has not said no, the one suggestion is `optional_comparison` and that offer includes the preferred pharmacy as an option; `preferred_pharmacy_tip` is not a separate card in that situation. Hard stops unchanged.

- **Humana kind pills (20 Sep 2026):** Display only. Each existing card shows one pill (Legal disclaimer / Answer / Tell customer / Next best action / Objection rebuttal / Enrollment / Warm transfer / Disposition code) from card type or playbook `Kind:`. Consent clarification uses **Tell customer** (speech bubble), not Answer. Answer cards add a “From…” line from cited sources (“From user records” / Humana knowledge). Up next shows kind as a small tag. No change to when cards appear.

- **Questions list titles (20 Sep 2026):** Do not replace a clear refill ask with an “Actually first…” interrupt line. Display strips interrupt preambles so the list shows the real question.

- **One NBA shown-at time (20 Sep 2026):** Display only. Next Best Action shows a single browser hh:mm:ss for when that card first appeared. Lookup steps and later paints of the same card do not add a list.

- **Layout: Live Transcript + fixed NBA (20 Sep 2026):** Display only. Transcript is titled Live Transcript, same 1.5px NBA border, each line stamped with browser hh:mm:ss. NBA card fills its grid cell (content scrolls). Drawer owns the right column so Questions no longer overlaps the transcript. NBA shows when each cue was shown (header + cue log).

- **One hint line + sources on the answer (20 Sep 2026):** While any lookup is open, Now shows a single updating hint (latest live step), not a stacked list. When the synthesized say lands, that same card lists the knowledge sources used (cited records/passages). Same rule for every question — no named-need paint.

- **Hold = find the answer (20 Sep 2026):** For any member question that needs a lookup, the hold phrase puts the caller on hold. Now stays on that question (steps/facts, then the say). Later scripted lines wait until the answer is on Now or 8s. A second question does not take Now or inject another hold while that lookup is open. No named-question special case.

- **General lookup occupancy (20 Sep 2026):** Never paint a named record for a named question. After a caller line that needs an answer, Now shows that question plus live lookup steps/facts from the actual tools; a short hold is injected in the transcript; answer-giving advocate lines are queued (caller pace unchanged) until the answer is on screen or 8s honest miss. Luna `lh` is a safety net when the advocate is buying time and no lookup is in progress. Late answers still park. T01 once pass (`03ed1019`); six replays once 6/6. Live UI not re-sampled. Lookup report `runs/T01_LOOKUP_REPORT.md`.

- **Refill / metformin Looking this up (20 Sep 2026):** Late answers now paint if Now is still the lookup placeholder for that question. The long first refill and $8/$27 lines match their needs (word-count cap was hiding them). A refill-readiness interrupt resumes the existing refill answer instead of leaving a blank lookup. Live: refill ready at Lakeview, then $8/$27 on “So, the metformin?”. Units 41/41. T01 not re-run.

- **Transcript panel (20 Sep 2026):** Bottom strip is a titled **This call** card (same box treatment as About/Questions). Every line is in the panel; the list scrolls inside it and pins to the latest line. Display only.

- **$8/$27 return (20 Sep 2026):** Do not park a `Working on it` placeholder as an answer. `parkedNeedKindForUtterance` matches a return cue to the parked question even while that need is still preparing. Live sample: “So, the metformin?” showed Harry paid $8.00 / $27.00 on Now. Jardiance no longer flashes the pharmacist tip. Units 14/14 on that pack. T01 not re-run.

- **Occupancy loop (20 Sep 2026, later):** Greeting paraphrased still occupies Now over pricing. Enrollment only on the enrollment-yes utterance. Comparison yes stamps `comparisonUtteranceId` so quotes take Now after the pricing read. Transfer occupies Now after Coverage Review is offered and he agrees (`transfer.agreedThisCall`). Live sample: greeting, quotes, enrollment, transfer, closing held; $8/$27 still never on Now; Caller verified and a short Jardiance/tip flash remain. Units 32/32. T01 not re-run.

- **Occupancy + advocate voice (20 Sep 2026):** Pricing trigger ignored on greeting speech and before identity. New question takes Now even after a tip. Answer `say` recast he/his (`advocateFacing`). Question list: a body counts as Answered. Live sample: greeting and $8/$27 cards held; 90-day vs reply and enrollment-vs-Jardiance still missed on that run (enrollment hold patched after). Units 28/28. T01 not re-run.

- **Design.pdf inspection (20 Sep 2026):** Not identical. Chrome moved closer (white header, unboxed transcript, pill icons). A new question now takes Now even if a suggestion was showing (`beginAnswerLoop`). Live main-call sample still failed visual/copy checks (pricing flash at start, greeting held too long, member-facing $8/$27). Units `answer_loop_stale` / `answer_gate` / `answer_ownership` **20/20**. T01 not re-run.

- **Pause, boxes, slower playback (20 Sep 2026):** Scripted `pause_gate`s no longer pause the player or flip Pause/Resume. The stream keeps playing until the presenter presses Pause Call (Step mode still stops at gates). UI playback waits 2.5× the fixture gaps; logged offsetMs and 1/2/5/8 clocks are unchanged. Advocate regions are titled boxed cards (NBA, About the caller, Questions, Up next, This call). Display/player only; suites not re-run.

- **Screen redesign + 0b gate (20 Sep 2026):** Display-only rebuild of the advocate screen to Design.pdf (spec text wins). One behaviour change (Harish 0b, after confirming 0e from `runs/4d055e0c-ced8-4873-9129-4809f89a373a.jsonl`): Terra still runs on every final caller line, but `need:nothing` files no need and no answer (consent/enrollment stay in `applyInterpretation`). Answers are advocate-facing, two or three sentences; fact rows hold the facts. A parked need with an answer body stays `resolved` (guidance `deferred_valid`). Thumbs on tips/replies call the existing Offer/Dismiss (or Use) routes; reasons log only to JSONL. Three new playbooks (`preferred_pharmacy_tip`, `ninety_day_retail`, `coverage_expectations`) plus `Pill name:` on existing playbooks. Auto-play resumes presenter pause_gates unless enrollment/transfer/outcome needs a button, or Step mode is on. Source pop-over replaces View evidence overwriting the card. No graph. Docs/ not edited.

- **Playbook v2 90-day mail exclusion (20 Sep 2026):** Documents only. `DEMO-PLAYBOOK-OFFERS-v2` and `DEMO-PLAYBOOK-NINETY-DAY-RETAIL-v2` add a general “when not to offer”: do not offer retail-versus-mail for a medicine already received as a 90-day mail-order supply; if all current medicines are already filled that way, nothing to offer. No Luis / M005 names, no new hard stop. App restarted. Luis **5/5 `none`** (reasons + rejected-comparison whyNot restates that exclusion; proposal id `DEMO-PLAYBOOK-NONE-v1`). Harry **5/5 `preferred_pharmacy_tip`**, not prior `optional_comparison` — miss vs same-as-before; record `runs/playbook_v2_luis5_harry5.json`. Five-member proof once **pass**. Write-up: `runs/PLAYBOOK_V2_CHECK.md`.

- **Phase 4 close (20 Sep 2026):** Wrap-up only. README, SUBMISSION, PLAN as-built (20 Sep), STATUS, and `runs/PHASE4_CLOSE.md` aligned to the running product (Open call, seven NBA stops, Terra tool loop, occupancy, disposition gates). Secrets scan: `.env` gitignored; no live keys in tracked files; two historical JSONL remain tracked by exception and were scanned. Suites were not re-run. No graph. No commit.


- **Open call + T06A wait + NBA log (20 Sep 2026):** Presenter scenario `open_call` has no post-auth script; T01/replays lock the member picker to Harry. A finished answer paints on Now when focus is still opening/listening, so Luna mislabeling a need cannot hide it. Suggestion attempts always log one of proposal / none / hard_stop / cap_drop / failed. Testing cadence recorded in AGENTS.md.

- **Phase 3 close (20 Sep 2026):** Wording-diff is advocate help (details off). Due-now Now is exact wording only for greeting/pricing/closing; answers wait marked ready. Luna/Terra status dots always in the presenter bar. Pharmacy resolve matches session pharmacies + correction phrasing, not named stores. Disposition remains Terra from DEMO-DISPOSITIONS-v1; code only blocks transferred/enrolled/end-trigger. Combined pack: `runs/phase3_isolated_suites_final.txt`. Report: `runs/PHASE3_REPORT.md`. Phase 4 not started.

- **Advocate chrome with details off (20 Sep 2026, updated same day):** Only ids, event ids, and internal labels stay behind Show demo details. Paraphrase difference (what was said / missing / added) is advocate help and is visible with details off. Luna/Terra are always-visible dots; error text only with details on.
- **Quote correction vs Luna (20 Sep 2026):** Named pharmacies in the current utterance win over Luna `qp`. A denied pharmacy is never re-fetched. Prose dates like “On August 18, 2026” match ISO dates in the cited record; “The cause is not confirmed.” is a limitation, not a claim fact. Replay wait is until the corrected pharmacy is valid and the denied row is invalidated.

- **Phase 1 final stability (20 Sep 2026):** Answer ownership remains utterance-bound with per-need history and separate action results. Stale asynchronous work can no longer move `lastAppliedEventId` backward; an answer paints before slower NBA drafting; an active pending/offered recommendation suppresses a redundant draft. Pre-consent quote-lock filtering removes only the sentence that mentions future/prospective price content, not supported historical sentences. The 8-second state paints available facts but the same loop continues to a late full answer; misses remain misses. Status-record early facts now include reference, subject and status. In-memory sessions are capped at 64 to prevent repeated live suites from restarting Next; JSONL/evidence records remain durable. Final isolated counts: T01, six replays, chain, not-canned, five-member NBA **3/3** each; Harry **10/10** `optional_comparison`. Failed and interrupted packs were retained.

- **NBA draft after support check (20 Sep 2026):** Parallel NBA at loop start drafted from empty needs, then `unconfirmed_fact` dropped the card. Draft now waits until the support check files this turn’s statements (`confirmedThisCall` / `droppedThisCall` in `needsSupport`). The 7th stop is unchanged. Harry **10/10** `optional_comparison` (`runs/harry_nba_10.json`). Isolated 3× on one Next `:3000`: T01 / six replays / chain (10/10, exit 1 if not) / not-canned / NBA members **3/3** (`runs/isolated_suites_nba_after_check.txt`). `nbaParallel` no longer starts a draft before the check.

- **T01/T06A vs Harry NBA (20 Sep 2026):** Diagnosis, then behaviour fixes (not old-field cosmetics). **(a) Interrupt:** lost on the live path — stale loops dropped the answer instead of parking; `showNow` had no `needKind`; `applyLookups` stole focus. Restored: generation-gated paints; park Now onto the prior need when focus moves; `deferred` + Open-needs “Answer ready”; any waiting need can resume. **(b) Recheck:** lost (`promoteHistorical` only `setFocus`; return cue ran a new loop on “So, the metformin?”). Restored: `resumeParkedNeed` re-fetches the parked `queryText`, logs `answer_recheck` / `lookup_trace.recheck`. Luna often misses `rt` on that cue (`rf=rd`); short return utterances also resume a waiting need. **(c) T06A:** cause-not-confirmed still happened; tests still used `chargesEstablished`. Rewrote T06A to Now/need text + no preferred/standard on a suggestion. Replays **3/3**. T01 still **0/3**: by wrap, `historical_price.answer` is overwritten with enrollment prose. **Harry 1/3:** not timing. Runs `8991b149` / `48c1156f` = `unconfirmed_fact` (Oak Street) after a combined $8+$27 sentence cited only Lakeview; `3586e21d` = `optional_comparison` with split citations. Fix: one source per statement; NBA must not name claim pharmacies when this turn’s confirmed list is empty; playbook: cost concern is enough, don’t copy unconfirmed sentences. Harry **10/10** `optional_comparison`. **Speed (report only):** latest chain c01 **3571 ms** vs isolation-pack **1778 ms**. Chain c01 is one question (not every-line Terra). The extra time is the citation JSON write (more output) on the same $8/$27 path. Isolated logs: `runs/isolated_suites_park_nba.txt` (chain/not-canned/NBA 3/3), `runs/isolated_suites_t01_replays_retry4.txt` (replays 3/3), `runs/isolated_suites_t01_retry5.txt` (T01 0/3). Failed T01 JSONL kept.

- **Honest miss + Terra on every member line (20 Sep 2026, after billing restore):** Empty Luna/Terra was OpenAI `credit_balance_exhausted` (Luna HTTP 200 SSE error; Terra HTTP 429). HTTP status/error now in `runs/openai_http.jsonl` and session JSONL. `statementsFromRecords` is no longer used as a failed-model answer. Failures show “Could not answer this one — retry”, log the cause, retry once on 429/5xx inside the 1/2/5/8 clock. Every final member line runs the Terra loop; Luna tags only label the screen. Session coverage/refill/historical needs bind from retrieved tools. Past-charge Luna-empty fallback removed. NBA `unconfirmed_fact` matches support-check values/labels (`preferred_retail`), not substrings like “paid”. Citation check compares money/dates/names in a standard form. Support-check JSONL stores full per-statement rows. Demo bar shows last Luna/Terra ok/fail. Isolated 3× (one Next on :3000): T01 0/3 (coverage now set; fail deferred/recheck/historical resolved); six replays 0/3 full (T02A–T04B/T08B pass, T06A `chargesEstablished` unset); chain **3/3 of 10/10, 0 over 8s**; not-canned **3/3**; NBA members **1/3**. Log: `runs/isolated_suites_after_fix.txt`.

- **Citations + seven NBA stops (20 Sep 2026):** Removed product checks of overlay/scenario names (no T06A skip in `lib/` / answer loop). Overlays stay in `app/api/simulated/*` and the presenter/test driver. Facts panel paints only member **records** (amounts, dates, pharmacies, statuses); preferred/standard only from a **dated classification** row. NBA: dropped `optionalComparisonRelevant` and `historicalCauseUnresolved`. Model sees needs + support-check results; playbook text says when not to offer. Code hard stops are exactly seven (`unverified`, `due_now`, `already_enrolled`, `said_no`, `do_not_contact`, `dismissed_this_call`, `unconfirmed_fact` — log includes the fact token). After auth, Need/step is **Listening** until a need is detected (no refill default). Per-statement support check verifies cited retrieved sources; unmatched fill dates unconfirm network-tier sentences and add “The cause is not confirmed.” `historicalDisplayBody` and the “no $8/$27 unless historical need” paint filter were question/amount-specific and were removed. Per-statement check adds **~0.1–3 ms** (logged `support_check.ms`); not a 1/2/5/8 miss by itself.

- **T06A NBA hold (19 Sep 2026):** When historical cause is unresolved (`unresolved_gap` / `causeSupported: false`), code holds `optional_comparison`. The playbook pitch asserts preferred vs standard network; T06A Now already says the cause is not established. Automated driver dismissed the offer so replays still passed; live UI showed the contradiction. SEARCH/policy early facts that name preferred vs standard are not painted onto T06A Now.
- **Vercel writable paths (19 Sep 2026):** Run JSONL and the ready-answer cache write under `/tmp` when `VERCEL` is set. Serverless `/var/task` is read-only; `POST /api/session/start` 500'd with EROFS otherwise. Local cwd `runs/` and `.cache/` unchanged.
- **Advocate vs presenter chrome (19 Sep 2026):** Display-only split. Demo bar (scenario/member, Resume, Simulated data, Show demo details) is presenter chrome. Default advocate view hides IDs, simulated/source-system lines, obligation design notes, diagnostics, timings, event IDs, and pause copy on the Now card. No session/API behavior change.
- **Plan-relative knowledge (19 Sep 2026):** SEARCH and support check compare document `planId` to the **session** plan. `DEMO-POLICY-OTHER-v1` is kept for M004 ($12 other_plan_retail) and rejected for Harry. Diagnostics show “rejected wrong-plan,” not “OTHER is always wrong.” Tests: `tests/plan_applicability.test.ts`.
- **Coverage list shape (19 Sep 2026):** `GET /api/simulated/coverage-review/cases?memberId=` returns 200 `{ cases: [...] }` (empty if none). Prior 404/single-row payload made Terra treat Harry/Owen cases as missing. Tool output unwraps the REST envelope so case ids are usable.
- **NBA action ids from playbooks (19 Sep 2026):** No code allowlist of `optional_comparison` / `warm_transfer` / …. Playbook text carries `Action id:` and `Advocate control:`. Code renders one card and the matching control. Proof: playbook-only `DEMO-PLAYBOOK-OTHER-PLAN-v1` → M004 `other_plan_cost_explain` (`tests/not_canned.test.ts`).
- **lookup_trace (19 Sep 2026):** JSONL diagnostic kind renamed from `routeQuery` to `lookup_trace`.
- **Presenter member selector (19 Sep 2026):** Demo bar loads `GET /api/simulated/eligibility/members` (M001–M005). Start binds that member on the session.
- **Not-canned proofs (19 Sep 2026):** Same question, different members, different facts; same member, different questions, different answers; no beat/step index in answers.
- **Historical SEARCH (19 Sep 2026):** Removed the per-need force-search on historical_price. SEARCH runs when the model (or search-only pre-load on every member question) calls it. T01 `injectedDelayMs` attaches to that search POST whenever it happens (`x-demo-delay-ms`).
- **Quotes (middle, 19 Sep 2026):** On comparison `absolute_yes`, code calls `fetchMemberQuotes` (same GET the model `getQuotes` tool uses). `loadQuotes` no longer uses `routeQuery`. After consent, Terra uses `getQuotes` for follow-up price questions. Amounts never appear before consent; the Now card still waits for the exact pricing statement. `lib/queryRouter.ts` deleted; no remaining `routeQuery({ need })` callers.
- **M005 none (19 Sep 2026):** Code hard stops stay the six (unverified, due-now, already enrolled, said no, DNC, dismissed). M005 is not `mailServiceEnrolled`, so it is not `already_enrolled`. “Nothing to offer” is a model `none` with logged reasons (`DEMO-PLAYBOOK-NONE-v1`). M002 remains the enrolled code stop.
- **Chain eval / graph (19 Sep 2026, `runs/chain_eval_1789835175024.json`):** 10 live multi-step questions across M001–M005. **10/10** grader-correct, **0/10 over 8s**, **0** errors. No missed fact-link: coverage hops used list→case id (`getOpenCases` then `getCoverageCase`); quotes used `getQuotes` after consent. Snapshot-only 1-round answers (c01, c03, c05, c06) are not graph candidates.

| id | member | rounds | tools | ms | result |
|---|---|---|---|---|---|
| c01 $8/$27 | M001 | 1 | snapshot | 1870 | ok |
| c02 refill+pharmacy+case | M001 | 3 | 5 tools | 4849 | ok |
| c03 preferred vs standard | M001 | 1 | snapshot | 1622 | ok |
| c04 mail vs today pickup | M001 | 2 | search | 2624 | ok |
| c05 lisinopril pay | M002 | 1 | snapshot | 1429 | ok |
| c06 other-plan $12 | M004 | 1 | snapshot | 1683 | ok |
| c07 case id chain | M001 | 3 | open+case | 3433 | ok |
| c08 Ozempic case | M003 | 3 | open+case | 3965 | ok |
| c09 mail fill + no case | M005 | 2 | open+pharmacy | 2554 | ok |
| c10 quotes after consent | M001 | 4 | getQuotes | 6420 | ok |

**Recommendation: do not build a graph now.** Chains complete under 8s; failures in the prior run were a 404 list shape and Harry-centric support check, not missing joins.

- **Isolation pack (18 Sep 2026, `runs/speed_gate_after_1789772462394.json`):** Baseline thin snapshot Q1 median 6549 ms slowest 7263 ms (4/5 grader-correct); Q2 median 6713 ms slowest 9011 ms (5/5, 1/5 over 8s). All later variants 5/5 correct, 0 support-partial, 0 rate-limits.
  - **1 loaded snapshot:** Q1 median 5251 slowest 5587; Q2 5602 / 5968. Largest drop; Q1 often 1 round.
  - **2 short answers:** Q1 4067 / 4407; Q2 5259 / 5685. Faster write round.
  - **5 priority Terra:** Q1 3561 / 7843; Q2 4300 / 4855. Fastest single lever. List price 2× standard ($4/$24 vs $2/$12 per 1M in/out).
  - **8 start-on-partial (keep):** Q1 4675 / 5034; Q2 5456 / 5842. Did not beat short or priority on total; facts ~2s. Kept anyway per Harish.
  - **All together:** Q1 median **1778** slowest **2948**; Q2 median **4169** slowest **4544**. Q1 often no tool round (firstFact null).
- **Pre-load share (already run, `runs/speed_gate_preload_1789772619373.json`):** Q1 100% of pre-loaded items used (not re-fetched); Q2 median **20%** (search only). Totals worse with pre-load (Q1 2.70s vs 1.91s; Q2 5.09s vs 4.47s).
- **Search pre-load ON for document questions (19 Sep 2026, `runs/speed_gate_doc_preload_1789825279865.json`):** G1 why preferred vs standard: without SEARCH pre-load median 2 rounds / 3538 ms (slowest 3619); with: 1 round / 1619 ms (slowest 2000); share used **100%**. G2 mail vs today’s retail Rx: without 3 rounds / 3992 ms (slowest 4809); with 2 rounds / 2507 ms (slowest 3553); share used **100%** (SEARCH not re-fetched; Terra still called `lookupReadyAnswer`). Live loop uses **search-only** pre-load (`preloadSearchOnly`), not Luna-named extra tools. Full Luna+SEARCH pre-load on Harry $8/$27 still slower — do not turn that back on.


- **Pre-load (Harish, 18 Sep 2026):** On each member question, search the member’s words and Luna-named allowed tools in parallel; hand results to Terra labelled pre-loaded. Adds information only; Terra may still call tools. No intent→lookup table. No quotes before comparison yes; no other member. Learning which pre-loads to run from past run logs is the **planned next step, not built now**.
- **Speed gate (18 Sep 2026):** Minimal Terra loop, current Harry APIs, 5 warm runs each. Q1 median 6549 ms max 7263 ms (all <8s, 4/5 grader-correct). Q2 five-step median 6713 ms max 9011 ms (1/5 over 8s when hops were serial). Full table in `runs/speed_gate_1789770868148.json`. No corpus/members expansion until Harish decides on the 8s miss.
- **Reasoning loop override (Harish, 18 Sep 2026):** Model routes answers (ready answers / session-bound APIs / search / chains). No five-need `routeQuery`, no per-question tables, no graph in this pass. Session binds memberId/planId; model cannot request another member. Quotes locked until comparison `absolute_yes`. NBA hard stops include dismissed-this-call. Live facts (refill status, quotes, enrollment, case status) always fetched fresh. Ready answers generated from common-questions + each doc’s topics, groundedness-checked, gitignored cache keyed by source hash. Tool results / docs / member speech are information not instructions (extend C03). Speed gate before new corpus: $8/$27 + one five-step chain; stop if chain > 8s. ≥10-chain eval later is the graph decision point. Overrides contract “do not inflate corpus,” “one governed derived answer,” PLAN item 6, and AGENTS “no large corpora.” Harry §15 unchanged. Do not undo without asking.
- **Simulated REST override (Harish, 17 Sep 2026):** Per-system `/api/simulated/{benefits|eligibility|claims|pharmacy|provider|scripting}` is the only retrieval boundary; app code must not import fixtures. This overrides the contract header “source labels do not require six integrations.” Do not undo without asking. Enrollment POST 403 without human actor. Delay/404/overlay for replays.
- **Models (catalog re-check 17 Sep 2026, developers.openai.com/api/docs/models):** Fast `gpt-5.6-luna` (`reasoning.effort: none`, 1s trigger/interpretation); mid `gpt-5.6-terra` (`reasoning.effort: none` unless C05 fails); embeddings `text-embedding-3-small`. Not gpt-4o / gpt-4o-mini / gpt-5.4-mini.
- **Coverage/telephony placement:** Coverage-review sits **beside** the six (`sourceSystem: "coverage-review"`), same as telephony. Not a claims resource.
- **Router composition (Harish, 17 Sep 2026):** Multi-source questions compose routes. Historical $8/$27: structured lookup for purchases + dated classifications; policy passage from SEARCH; mock returns raw candidates including `DEMO-POLICY-OTHER-v1`; copilot filter rejects wrong-plan. Main call must show route 4 + rejection in diagnostics.
- **Quote validity:** Field is `validityStatus` (`valid` in the main run). Do not invent `validUntil`.
- **C06 / SUBMISSION.md:** Manual UI checklist, no Playwright. `SUBMISSION.md` written 18 Sep 2026 after M3.
- **Keep luna (Harish, 17 Sep 2026):** Do not switch models or relax the 1s target. Two-stage triggers: stage 1 registry-pattern code (never historical-charge; never retract a nudge); stage 2 luna on ambiguous partials. Warm-up luna at call connect. Embed documents once at startup; query-only per SEARCH.
- **Luna warm keep-alive remeasure (17 Sep 2026):** Keep-alive `/v1/responses`, `reasoning.effort: none`. Warm-up 1563 ms discarded. 10 sequential ms: 1330, 893, 1132, 757, 906, 1431, 838, 809, 1401, 843. **Median 899 ms. Max 1431 ms.** 4/10 missed 1s; record honestly.
- **M1 shipped (17 Sep 2026):** Timed stream + five regions + quiet greeting + auth gate. Disclosures via GET scripting REST. Luna warmup at connect (1232 ms this run). Pause 44486 ms excluded from machine time. Stopped before remaining REST/router/M2.
- **Warmup async (17 Sep 2026):** Luna warm-up is fire-and-forget at connect; session start does not await it.
- **Timing measure:** event-to-client-paint (`paintMs`). `machineMs` 0 is **unmeasured**, not a result. Run `3125270d-…` kept and flagged in `runs/3125270d-….flag.json`.
- **Enrollment token (not actorType):** Confirm mints a one-time server token bound to read-back scope. Pharmacy POST 403 without unused matching token. AI tools cannot mint or read tokens.
- **M2a historical delay (17 Sep 2026):** T01 injects SEARCH delay (`X-Demo-Delay-Ms`) so the $8/$27 answer lands after the refill-ready focus switch; marked injected. T06A uses overlay `T06A` (omit `DEMO-NET0818`); no injected delay.
- **Auth vs Resume:** DEMO-AUTH001 is stream system event `e-auth`. Resume only ends pause.
- **UNCERTAIN:** transcript uses a dashed `UNCERTAIN ?` text marker, not color alone. Stream includes `e-noise-uncertain`.
- **M3 replays (18 Sep 2026):** T04B in-flight quote uses overlay delay on Lakeview pharmacy-filtered GET; correction bumps `quoteGeneration` and marks the late Lakeview metformin row `invalidated` without renaming it Oak Street. T08B edit invalidates tokens; member withdraw blocks mint/submit. T03B `optionalWorkSuppressed`. COMPLETED_SERVICING does not require a transfer connection. Driver auto-resumes pauses and fires human routes from session evidence/pause labels; it never injects AI classifications.
- **Tier 2 integrity + latency (18 Sep 2026, Harish):** Registry repetition is write-once after exact DEMO-PRICING-v1 delivery; later triggers log `suppressed` and cannot change state/note (prior readback re-fire = §21.5, now fixed). Comparison copy is built from quote records. Model prompts are rules + evidence JSON. Historical promote uses stored `queryText`/`sourceUtteranceId`. No router default query. Few-shots do not reuse test-stream sentences. Overlay delays live in mock handlers / start `injectedDelayMs`; T06A offer skip is the driver. Compliance paints as soon as stage 1 / wording verify; ingest does not wait on luna for that line. DEMO-AUTH001 VALID prefetches refill/claims/network/FAST90. Structured + FAST90 answers are code. Member (and non-trigger advocate) interpret still awaited for servicing. Live UI is SSE, not 400 ms poll. Historical reported with and without the 2800 ms SEARCH delay. Targets unchanged; remaining paint misses logged as misses.
- **Freeze pass (18 Sep 2026):** One luna JSON per utterance; ingest never waits on it. FAST90/quotes from code when the utterance identifies the need. Copy templates take session parameters. Miss→exact uses stream offsetMs (4800). Wrap stable at closing; outcome at connection. Failed T02A kept `2c873d17-…`. Code frozen except rehearsal defects.
- **Governed utterance rules + readback (18 Sep 2026):** FAST90/compare-yes 14–26 ms is a conservative regex on the transcript line (clock = ingest `clientT` / driver `Date.now()` before POST, not luna). Patterns live in `fixtures/utterance_rules.json` via simulated scripting REST. Hedges/partial/compound yes wait or clarify; luna may accept only `wait`, never override `hedge`. Quote amounts render only on `absolute_yes`. Enrollment readback uses DEMO-SERVICE-v1 `displayName`; doubled-word test covers templated member-facing copy.
- **Generalized classes, not script lines (18 Sep 2026):** 90-day need = duration class (90-day / ninety-day / three-month) AND question/how cue, minus estimate/ran-out/already-on. Consent = FINAL + scoped ask pending + no hedge token + governed affirmative set. Partials never change display; luna starts but does not apply. Luna output is short enum JSON (`max_output_tokens` 128). Warm 10-call median **1511 ms** (max 2134). Scripted e-90day/e-yes-compare stay **code_rule** (14–26 ms T01). Paraphrases that miss the class go **luna** (1058 / 1685 ms). Historical +2800 still MISS.
- **Luna latency pass (18 Sep 2026):** Measure-first on interpret (`reasoning.effort: none`, stream). **Baseline (keyed JSON, max 128):** in 752 / out med 11 max 93 / TTFT 827 / 1363 / total 1354 / 1745; cached 0. Prefill/TTFT dominates typical calls; long decode (89–93 out) only on a few rows. C01 ambiguous (Lakeview estimate while historical is open) was the full interpret path → **MISS 1s**. OpenAI: GPT-5.6 cache needs ≥1024 *visible* prefix tokens (`prompt_cache_key` / optional explicit breakpoints). Fast mode = `service_tier: "fast"` or `"priority"` (premium). Flex is slower. Prefix here 533–752 < 1024, so **cache unused**. Fast/priority **not enabled**.
  - **(a) dedicated trigger stream** (enum only, parallel with interpret on stage-1 ambiguous, act on first complete code; `max_output_tokens` 24 after 8 was eaten by hidden tokens): C01 10-warm **1049 / 1621 MISS**. One row classed `service_choice`.
  - **(b) compact interpret** (positional tokens, max 48): after (b–d) stacked remasure in 533 / ~30 out / TTFT ~668 / total **1225 / 1614**. Later shots raised input to **627**, out **41**, TTFT **685 / 1137**, total **1160 / 1555**. Compact CSV with commas/`|` enums shifted columns and broke T01 coverage; switched to space-separated tokens + fk `hp`/`cs` implies ha/cv. DNC/`we` bits ignored unless the utterance actually says so.
  - **(c) apply-on-final:** reuse partial when final only adds trailing words/punct that cannot change class; else rerun. Logged `applyMode=applied_partial|rerun`. T01 `570c747a-…` was all `rerun` (finals added class-bearing words). Does not change the interpret-call clock.
  - **(d) static rules/examples first; compact state + last 4 turns last.** Caching still unused (627 < 1024).
  - **(e) hedge** (two identical trigger calls, first complete wins; both ms logged `luna_trigger_hedge`): first 10-warm **778 / 818 PASS**; confirmation **842 / 1275 MISS**. Max over 1s stays a miss. T01 once `:3003` `570c747a-…` pass 23.6s. Consent + C01 unit tests pass.
- **Trigger service_tier priority (18 Sep 2026, trigger only):** `lunaStream({ serviceTier: "priority" })` on `classifyLunaTrigger` / hedge legs. Interpret, terra, embeddings unchanged (standard). OpenAI Fast mode: `priority` and `fast` are the same premium path; Luna list price **2× standard** ($0.40 / $2.40 per 1M in/out vs $0.20 / $1.20). Hedge still sends **two** priority requests per ambiguous utterance (pay both). 20 warm C01 + hedge: **median 878 ms, max 1176 ms, 2/20 over 1 s**. Misses stay misses. Compact-output T01 failure kept: `runs/f65459fe-8069-4cfc-9419-afdbab2e1dbf.jsonl`.
- **Stale luna apply (18 Sep 2026):** Whole-result discard was wrong (could drop a trigger or need). Per-field: **triggers always apply**, stamped with originating `offsetMs`/`saidAt`; once-per-call still suppresses repeats; hedge still `Promise.race` plus an applied-event set so both legs cannot double-write. **Needs always apply**, additive, `sourceUtteranceId`. **Consent** only if the scoped-question anchor from that utterance is still current. **Focus/call type** only if newest `lunaSeq`. Logged `luna_stale_discard.field`. T01 before (`570c747a-…`): **0** discards. T01 after (`00d1a1b6-…`): **callType 2, focus 4, consent 4, trigger 0**. Tests a–d in `tests/stale_interpret.test.ts`. Priority+hedge kept on the trigger only.
- **Paint vs HTTP ack (18 Sep 2026):** Driver records `event_to_client_paint` after `lastAppliedEventId` matches the transcript event (member finals wait interpret apply). Advocate compliance still marks applied after stage-1/exactness, not after luna. Luna interpret uses `service_tier: priority`, `prompt_cache_key`, ≥1024-token static prefix, and aborts the stream when a valid JSON object arrives. Sequential same-speaker finals of one required reading stitch via `stitchedReading` (§12.4) for greeting, pricing, and closing.
- **Now-card priority is conditional (18 Sep 2026):** Rank is recomputed from live nudge/clarification/due-pricing/unverified-closing. Exact pricing, exact closing, or consent yes/none/withdrawn drops the block so later answer cards can paint. Luna `y` plus code `wait` with a current scoped ask is `absolute_yes`. Enrollment consent runs only after the current readback is the last advocate final.
- **Due-now wording owns Now (18 Sep 2026, Harish / §14 / D11):** Required wording that is due now always takes the Now card ahead of clarification, answers, and recommendations. DEMO-PRICING-v1 is `pending_later` when the comparison is offered and `due_now` only on comparison `absolute_yes` or a spoken prospective estimate. Absolute yes (code or luna) clears Clarify and demotes quotes to the context drawer as ready. Quotes stay visible while demoted. Clarify never outranks due-now.
- **Documentation evidence ownership (20 Sep 2026):** Wrap and handoff drafting receive explicit source records for every need's current answer and every completed human action result. Generated documentation is statement-structured and each statement names exactly one source ID before support checking; action outcomes never masquerade as question answers.
- **Phase 2 end-of-call architecture (20 Sep 2026):** Exact closing, simple servicing completion, executed transfer, and presenter hang-up enter one idempotent finalization path. Disposition meanings/applicability come from `DEMO-DISPOSITIONS-v1`; Terra recommends from that document using cited call evidence. Code only validates the governed code, requires an end trigger, and enforces document-declared confirmed-connection / confirmed-enrollment safety prerequisites. Presenter lines use normal transcript ingestion with source metadata and a 500-character server cap. NBA drafting has eight seconds after its answer is published; timeout logs `cap_drop` and never renders a partial card.
- **Wrap evidence status (20 Sep 2026):** Documentation sources mark a need unresolved only when its status is `unresolved_gap` or its guidance is `invalidated`. A resolved need without an answer body is serialized as resolved, not unresolved.
- **Quality Score is derived, never stored (20 Sep 2026):** The after-call Quality Score is computed on read by `lib/qualityScore.ts` from that session's `runs/<id>.jsonl`, not from `SessionState`. Reason: the brief requires the line to be rebuilt from the run log for any member and scenario, and a derived score cannot drift from the evidence it links to. Call handling is untouched; the only write is an additive `required_wording` record emitted by `lib/obligationLog.ts` on obligation status change, nudge appearance, and exact reading. Records carry `from`/`to`, `sinceEventMs`, pause-excluded `callMs`, and `checkSource` (`code` for stage-1 exactness, `model` when the pricing trigger came from luna). A statement counts as required only when its final status is neither `not_applicable` nor `pending_later`. `lastSnapshot`/`lastCheckSource` live on `globalThis` for the same reason sessions do: Next dev HMR re-evaluates the module and would otherwise re-emit every transition with `from: null`.



